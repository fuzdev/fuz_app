---
'@fuzdev/fuz_app': minor
---

refactor!: rename `auth/route_guards.ts` → `auth/auth_guard_resolver.ts`; import path is the only consumer-visible change (export name `fuz_auth_guard_resolver` unchanged)
