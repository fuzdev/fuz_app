---
'@fuzdev/fuz_app': minor
---

add `create_broadcast_api` for backend-initiated JSON-RPC notifications, with optional per-connection `should_deliver` ACL hook; add `BackendWebsocketTransport.broadcast_filtered`
