---
'@fuzdev/fuz_app': minor
---

feat(testing): fresh-keeper-per-test cross-process model — `_testing_reset` fires unconditionally per `setup_test()` and inline-seeds a fresh keeper, converging in-process and cross-process keeper lifetimes. `extra_keeper_roles` becomes bootstrap-time additive on both transports (no per-role offer/accept cost cross-process); new `extra_accounts: [{username, roles}]` cradle on `default_in_process_setup` + `default_cross_process_setup` for bootstrap-only roles like `ROLE_KEEPER`; fixture exposes `extra_accounts[username]` accessor. Test-binary factory renamed `create_testing_reset_actions` → `create_testing_actions(deps, {session_options, daemon_token_state, reset_state?})` — consumers wiring test binaries update their `_testing_reset` registration. Invite-gated per-test signup via `fixture.create_account()` keeps `open_signup: false` at production default cross-process; post-bootstrap `create_account({roles})` rides production `role_grant_offer_create` + `_accept`. Capability-gated in-process-only role-grant audit tests lifted to offer/accept so they run cross-process too; `ROLE_KEEPER` probe restored as a runtime-skipping `keeper-only account cannot list accounts (keeper ≠ admin)` test that activates when consumers wire `extra_accounts: [{username: 'non_admin_keeper', roles: [ROLE_KEEPER]}]`.
