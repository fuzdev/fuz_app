---
'@fuzdev/fuz_app': minor
---

feat: add `create_frontend_rpc_client` (typed Proxy bundle with default `FrontendHttpTransport`) and `all_standard_action_specs` (admin + permit_offer + account spec aggregate mirroring `create_standard_rpc_actions`)
